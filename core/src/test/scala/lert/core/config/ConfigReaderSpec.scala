package lert.core.config

import lert.core.BaseSpec

class ConfigReaderSpec extends BaseSpec {
  it should "read a json config" in {
    //val config = new JsonConfigReader(this.getClass.getClassLoader.getResourceAsStream("rest-config.json")).read()
  }
}
